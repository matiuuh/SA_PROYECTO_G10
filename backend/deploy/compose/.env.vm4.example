# Variables de entorno para VM4 (api-gateway · frontend)
# Copiar a .env en /opt/quetzaltv/backend/deploy/compose/ y rellenar los valores vacíos.
# IPs internas de la VPC de GCP.

VM1_HOST=10.128.0.2
VM2_HOST=10.128.0.3
JWT_SECRET=
AR_REGISTRY=us-central1-docker.pkg.dev/sa-proyecto-g10/quetzaltv
IMAGE_TAG=latest
